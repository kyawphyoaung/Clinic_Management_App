# Phase 707_06 – Invoice, Country Fix, Search, Pagination & UI Refinements

## Invoice UI Improvements

### Invoice Page Layout
- Change "Date" label to **"Issue Date"**
- Add a footer to the invoice with:
  - Clinic logo (small size)
  - Below logo: `www.revivoratw.com`
  - Right side: `For more information or any issues or concerns, email us at uroadrian.tw@gmail.com`
- Change headline from "INVOICE" to **"Medical Billing Invoice"**
- Add background watermark:
  - Use `main_logo.svg`
  - Convert to black & white
  - Reduce opacity
  - Place as watermark on the invoice background
- Background color: Use a suitable hospital blue tone

### Invoice Save Flow (`/dashboard/treatments/[id]/invoices/new`)
- Replace `Save & Print Invoice` button with **`Save`** button only
- After Save:
  - Show: **"Invoice Created Successfully"**
  - Display two buttons:
    - **Print Invoice**
    - **Done**
- Clicking **Done**: Redirect back to the previous page automatically
- Clicking **Print Invoice**: Open print dialog (browser print popup)

### Invoice Table (`/dashboard/treatments/[id]`)
- Remove **View** and **Update** buttons (do not use two separate buttons)
- Keep only **View** button (not a modal popup)
- Clicking View opens a **full page** (same UI as Add Invoice page, not a modal)
- The View page should allow:
  - Add lines
  - Remove lines
  - Edit all invoice fields
- Save button → **Update** button
- After clicking Update:
  - Show alert: **"Old Invoice Total: [amount], New Invoice Total: [amount]. Are you sure?"**
  - Confirm → Show: **"Update Successfully"**
  - Then show: **Print Invoice** and **Done** buttons

---

## Country Code & Demographics Fix

### Issue: Country Code Stored as "MM"
- Currently, `Patient.country` is stored as `MM` (abbreviation)
- This should be stored as full country name: `Myanmar`, `Taiwan`, `United States`, etc.
- **Apply to:** Both `nationality` and `countryOfResidence` fields
- Also fix all existing dummy data in the database
- Create a migration script or a one-time run script to update existing records

### Issue: Patient Registration Form Address Fields
- Registration form fields:
  - Country of Residence (required)
  - Street Address
  - City
  - State/Province
  - Postal Code
- **Street Address** and **City** should be **required**
- All these fields must appear in `/dashboard/patients/[id]` → Demographics → Section A – Personal Information
- **Fix:** Ensure all fields from the registration form are displayed in the patient detail page

---

## Payment Receipt UI

### Payment Modal (`Make New Payment`)
- Change `Confirm & Print Receipt` button to **`Confirm`** only

### Payment Record Row – Receipt Button
- Each payment row should have a **Receipt** button
- Clicking Receipt should **NOT** use `/dashboard/treatments/[id]/receipt/[id]`
- Use the **same UI as Invoice** (not the old receipt page)
- Add a `paid.webp` transparent image on top of the receipt (below Total Amount)
- Paper size view with:
  - **Print** button (opens browser print dialog)
  - **Done** button (returns to previous page)

---

## Body Diagram / SOAP Notes Fix

### Image Paths
| Diagram Type | Image Path |
| :--- | :--- |
| **Full Body** | `public/images/body/fullBody.webp` |
| **Urology** | `public/images/body/male_female_urology.webp` |
| **Brain** → Rename to **Neurology** | `public/images/body/neurology.webp` |

### Schema Fix
- Fix the Prisma schema error when saving SOAP notes
- Ensure `diagramType` and `pins` fields are properly defined
- Run migration to sync schema with the database

---

## Cashflow Page (`/dashboard/cashflow`)

### Issue: Chart Missing
- Monthly inflow & outflow chart no longer appears
- It was visible before, but is now missing
- **Fix:** Investigate and restore the chart
- Ensure chart data is correctly passed and rendered

### Multi-Currency Inflow Table
- Currently shows `Country` column with `unknown` for TWD rows
- **Remove the Country column entirely**
- Show only:
  - Currency
  - Amount
  - TWD equivalent

### Patient Payment Status Table
- Add **pagination** to this table
- Show: `Showing 1–20 of 48` (example)
- Navigation: `Previous Page 1/3 Next`
- **Rows per page:** Add a dropdown with options (e.g., 20, 50, 100)

---

## Patients Page (`/dashboard/patients`)

### Search & Filter Card
- **Search Box:** Make it full-width (longer) at the top
- Move the following filters to the **row below** the search box:
  - Status
  - Source
  - Agent
  - Sort by
- **Remove Rows per page** from this card (it should only appear at the bottom of tables)

### Search Suggestions
- When typing in the search box, show suggestions for:
  - Patient Name (partial match)
  - Patient ID (partial match)
  - Visit ID (partial match)
  - Phone (partial match)
- Suggestions should appear as a dropdown below the search box

---

## Treatments Page (`/dashboard/treatments`)

### Search & Filter Card
- **Search Box:** Make it full-width (longer) at the top
- **Remove Rows per page** from this card
- Search suggestions for:
  - Patient Name
  - Patient ID
  - Diagnosis

### Table Pagination
- Add pagination to the Treatments table
- Place pagination controls at the bottom of the table
- Include `Rows per page` dropdown (20, 50, 100)

---

## Partner Dashboard (`/partner/dashboard`)

### My Referred Patients Table
- Add **pagination** to this table
- Add a **search box** inside this section
- Add **reference/legend** below the table header:
  - **Deposit Amount:**
    - 🔴 Red dot = Awaiting Deposit
    - 🟢 Green dot = Deposit Received
  - **Commission Amount:**
    - 🟠 Orange = Under Review
    - 🟢 Green = Paid

### Monthly Commission History Table
- Add **pagination** to this table
- **No search box** needed

### Partner Dashboard Footer
- Add `main_logo.svg` (small)
- Below logo: `An online medical platform connecting patients with trusted care in Taipei`
- Contact: `uroadrian.tw@gmail.com`
- Style: Clean, small, minimal

---

## Summary of Changes

| Area | Change |
| :--- | :--- |
| Invoice UI | Improve design, footer, watermark, "Medical Billing Invoice" |
| Invoice Save Flow | Save → success → Print/Done buttons |
| Invoice View/Edit | Single View button → full page editor → Update with confirmation |
| Country Code | Fix from "MM" to "Myanmar" (all fields + existing data) |
| Demographics | Show all registration fields in patient detail; make Street Address & City required |
| Payment Receipt | Use Invoice UI + paid.webp watermark; Print + Done buttons |
| SOAP Notes | Use new image paths; fix schema/migration for diagramType & pins |
| Cashflow | Restore chart; remove Country column in multi-currency table |
| Pagination | Add pagination to Patient Payment Status table, Treatments table, Partner Dashboard tables |
| Search Suggestions | Add suggestion dropdowns for Patients and Treatments pages |
| Partner Dashboard | Add footer, legend/reference, search box, pagination |